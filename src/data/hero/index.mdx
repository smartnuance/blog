---
title: 'Full stack Developer & Education Inspirer'
subtitle: 'I am Simon'
linkTo: '/resume/'
linkText: 'View My Profile'
---

import Banner from '../../components/ui/Banner';

I'm a visionary for software engineering and a pragmatist in writing code. I love teaching how I build stuff with the click-clacks of my keyboard. Great engineering originates from a pencil.

export default function HeroBanner(props) {
  return (
    <Banner
      title={props._frontmatter.title}
      subtitle={props._frontmatter.subtitle}
      linkTo={props._frontmatter.linkTo}
      linkText={props._frontmatter.linkText}
    >
      {props.children}
    </Banner>
  );
}
